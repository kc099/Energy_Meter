# Core framework
Django==5.2.6

# API + utilities
djangorestframework==3.15.2

# Database driver (used via pymysql.install_as_MySQLdb)
PyMySQL==1.1.1

# Task queue + broker client
celery==5.3.6
redis==5.0.1

# HTTP client + crypto helpers used across the project
requests==2.32.3
cryptography==42.0.8

# Template helpers
django-widget-tweaks==1.5.0

# Production server
gunicorn==21.2.0
